const { server } = require('../lib/server');
const supertest = require('supertest');
const mockRequest = supertest(server);

describe('Server tests', () => {

test('Correct response to POST request')

test('Correct response to POST request')

test('Correct response to GET request')

test('Correct response to GET request')

test('Correctly UPDATEs object')

test('Correctly UPDATEs object')

test('Correctly DELETEs object')

test('Correctly DELETEs object')


)

})